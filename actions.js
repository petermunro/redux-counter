/**
 * ActionCreators
 * These should dispatch an action to the store.
 */

export function incAction() {
  // call dispatch() on the store, giving it an action with type 'INCREMENT'
}

export function decAction() {
  // call dispatch() on the store, giving it an action with type 'DECREMENT'
}
